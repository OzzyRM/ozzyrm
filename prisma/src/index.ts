/**
 * @deprecated Import from `@ozzyrm/core` instead.
 * Parsers are bundled in core since 0.2.0.
 */
export {
    PrismaParser,
    expandPrismaWatchPaths,
    loadPrismaSchema,
    prisma,
} from "@ozzyrm/core";
